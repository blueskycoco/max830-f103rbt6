source [find interface/stlink-v2.cfg]

transport select hla_swd

source [find target/stm32f1x.cfg]

reset_config srst_only
init

proc flash_image {} {
    reset halt
    flash write_image erase gcc/infrar.bin 0x08000000

    reset init
    reset
    shutdown
}

proc rst {} {
	reset
	shutdown
}
